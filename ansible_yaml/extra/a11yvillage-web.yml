http:
  routers:
    https:
      rule: Host(`(`host`)`)
      entrypoints: websecure
      tls:
        certresolver: (`certresolver`)
    http:
      rule: Host(`(`host`)`)
      entrypoints: webinsecure